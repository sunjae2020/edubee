SAFETY RULES:
- Read /db/schema.ts and LeadsPage.tsx first
- NEVER modify existing CAMP routes
- Report file list first, wait for "진행해"
- npx tsc --noEmit after every change

Create /server/src/routes/crm-leads.ts:
  GET    /api/crm/leads                      query: leadStatus, assignedStaffId, search, page
  GET    /api/crm/leads/kanban               group by leadStatus with counts
  GET    /api/crm/leads/:id                  include activities
  POST   /api/crm/leads                      auto-generate leadRefNumber
  PUT    /api/crm/leads/:id
  PATCH  /api/crm/leads/:id/status
  POST   /api/crm/leads/:id/activities
  POST   /api/crm/leads/:id/convert-to-quote

leadRefNumber generation:
  const ref = 'LD-' + Date.now().toString(36).toUpperCase().padStart(8, '0');

convert-to-quote logic:
  INSERT into quotes { quoteRefNumber: 'QTE-' + ..., leadId: lead.id, quoteStatus: 'Draft' }
  return new quote id
  redirect to /admin/crm/quotes/:newQuoteId

Build /client/src/pages/crm/LeadsPage.tsx:

Top-right toggle: [Table] [Kanban]

Kanban view — 5 columns:
  New | Open | In Progress | Qualified | Unqualified
  column header: name bold + count badge bg #F4F3F1 rounded-full
  card: bg white border 1px #E8E6E2 radius 12px padding 16px
    - lead ref 12px #A8A29E
    - name 14px bold #1C1917
    - inquiry type badge
    - budget A$XX,XXX
    - assigned staff avatar
    - created date 12px #A8A29E
  card click → detail page

Table view columns:
  Lead Ref | Contact | Inquiry Type | Budget(AUD) | Expected Start | Assigned Staff | Status | Actions

Detail page /admin/crm/leads/:id:
  Header: LD-XXXXXXXX + status badge
  Top-right: [Convert to Quote] primary | [Edit] secondary

  Tabs: Details | Activity Timeline | Tasks | Files

  Activity Timeline:
    vertical timeline, channel icon + date + description
    bottom: [+ Log Activity] form
      channel select: Call/Email/SMS/Meeting/WhatsApp/LINE/KakaoTalk
      datetime, description textarea, save button

Verify:
npx tsc --noEmit
Create 1 lead → confirm LD-XXXXXXXX assigned
Toggle Kanban ↔ Table
Add activity → SELECT * FROM lead_activities ORDER BY created_on DESC LIMIT 1;
Convert to Quote → confirm quote created
