⚠️ REPLIT ENVIRONMENT RULES — APPLY TO THIS ENTIRE SESSION:
- Server must bind to 0.0.0.0 (never localhost)
- All credentials via Replit Secrets only — never hardcode
- Use tsx (never ts-node)
- drizzle-kit push only for NEW tables — use ALTER TABLE for existing
- All async routes must use asyncHandler wrapper
- process.on uncaughtException handler required in index.ts

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 1: PRE-FLIGHT CHECK — RUN FIRST, REPORT RESULTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Before writing any code, run this diagnostic and show me the output:

1. Check required Secrets exist:
   const keys = ['DATABASE_URL','JWT_SECRET','SUPABASE_URL',
                 'SUPABASE_SERVICE_KEY','PORT'];
   keys.forEach(k => console.log(k, process.env[k] ? '✅' : '❌ MISSING'));

2. Test DB connection:
   npx tsx -e "
     import { Pool } from 'pg';
     const p = new Pool({ connectionString: process.env.DATABASE_URL });
     p.query('SELECT NOW()').then(r => {
       console.log('✅ DB connected:', r.rows[0].now); p.end();
     }).catch(e => { console.error('❌ DB error:', e.message); });
   "

3. List existing tables:
   npx tsx -e "
     import { Pool } from 'pg';
     const p = new Pool({ connectionString: process.env.DATABASE_URL });
     p.query(\`SELECT table_name FROM information_schema.tables
               WHERE table_schema='public' ORDER BY table_name\`)
      .then(r => { r.rows.forEach(x => console.log('-', x.table_name)); p.end(); });
   "

4. Check if account_ledger_entries already exists:
   If YES → skip STEP 2, go directly to STEP 3
   If NO  → continue with STEP 2

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 2: CREATE account_ledger_entries TABLE
(Only if it does NOT exist from pre-flight check)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Add to db/schema.ts:

export const accountLedgerEntries = pgTable('account_ledger_entries', {
  id:               uuid('id').primaryKey().defaultRandom(),
  accountId:        uuid('account_id').references(() => users.id).notNull(),
  sourceType:       varchar('source_type', { length: 50 }).notNull(),
  // 'invoice'|'transaction'|'receipt'|'settlement_mgt'
  // 'institute_mgt'|'hotel_mgt'|'pickup_mgt'|'tour_mgt'
  sourceId:         uuid('source_id').notNull(),
  contractId:       uuid('contract_id').references(() => contracts.id),
  entryType:        varchar('entry_type', { length: 10 }).notNull(),
  // 'debit' | 'credit'
  amount:           decimal('amount', { precision: 12, scale: 2 }).notNull(),
  currency:         varchar('currency', { length: 10 }).default('AUD'),
  originalAmount:   decimal('original_amount', { precision: 12, scale: 2 }),
  originalCurrency: varchar('original_currency', { length: 10 }),
  audEquivalent:    decimal('aud_equivalent', { precision: 12, scale: 2 }),
  exchangeRateToAud: decimal('exchange_rate_to_aud', { precision: 10, scale: 6 }),
  status:           varchar('status', { length: 20 }).default('pending'),
  // 'pending'|'confirmed'|'reversed'|'disputed'
  description:      varchar('description', { length: 500 }),
  entryDate:        date('entry_date').notNull(),
  createdBy:        uuid('created_by').references(() => users.id),
  createdAt:        timestamp('created_at').defaultNow(),
  updatedAt:        timestamp('updated_at').defaultNow(),
});

Run (NEW table only — safe):
  npx drizzle-kit push

Verify:
  npx tsx -e "
    import { Pool } from 'pg';
    const p = new Pool({ connectionString: process.env.DATABASE_URL });
    p.query('SELECT COUNT(*) FROM account_ledger_entries')
     .then(r => { console.log('✅ Table exists'); p.end(); })
     .catch(e => { console.error('❌', e.message); });
  "

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 3: ADD ledger_entry_id TO EXISTING TABLES
⚠️ DO NOT use drizzle-kit push here — use ALTER TABLE only
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Create db/migrations/001_add_ledger_refs.sql:

  ALTER TABLE invoices
    ADD COLUMN IF NOT EXISTS ledger_entry_id UUID
      REFERENCES account_ledger_entries(id);
  ALTER TABLE transactions
    ADD COLUMN IF NOT EXISTS ledger_entry_id UUID
      REFERENCES account_ledger_entries(id);
  ALTER TABLE receipts
    ADD COLUMN IF NOT EXISTS ledger_entry_id UUID
      REFERENCES account_ledger_entries(id);
  ALTER TABLE institute_mgt
    ADD COLUMN IF NOT EXISTS ledger_entry_id UUID
      REFERENCES account_ledger_entries(id);
  ALTER TABLE hotel_mgt
    ADD COLUMN IF NOT EXISTS ledger_entry_id UUID
      REFERENCES account_ledger_entries(id);
  ALTER TABLE pickup_mgt
    ADD COLUMN IF NOT EXISTS ledger_entry_id UUID
      REFERENCES account_ledger_entries(id);
  ALTER TABLE tour_mgt
    ADD COLUMN IF NOT EXISTS ledger_entry_id UUID
      REFERENCES account_ledger_entries(id);
  ALTER TABLE settlement_mgt
    ADD COLUMN IF NOT EXISTS ledger_entry_id UUID
      REFERENCES account_ledger_entries(id);

  CREATE INDEX IF NOT EXISTS idx_ledger_account
    ON account_ledger_entries(account_id, entry_date DESC);
  CREATE INDEX IF NOT EXISTS idx_ledger_source
    ON account_ledger_entries(source_type, source_id);
  CREATE INDEX IF NOT EXISTS idx_ledger_contract
    ON account_ledger_entries(contract_id);

Create db/runMigration.ts:
  import fs from 'fs';
  import path from 'path';
  import { Pool } from 'pg';

  const pool = new Pool({
    connectionString: process.env.DATABASE_URL,
    connectionTimeoutMillis: 5000,
  });
  pool.on('error', (err) => console.error('Pool error:', err.message));

  const file = process.argv[2];
  if (!file) {
    console.error('Usage: npx tsx db/runMigration.ts <filename.sql>');
    process.exit(1);
  }
  const sql = fs.readFileSync(
    path.join(__dirname, 'migrations', file), 'utf8'
  );
  pool.query(sql)
    .then(() => { console.log(`✅ Migration ${file} done`); pool.end(); })
    .catch(e => { console.error(`❌ Failed:`, e.message); pool.end(); process.exit(1); });

Run:
  npx tsx db/runMigration.ts 001_add_ledger_refs.sql

Also update db/schema.ts TypeScript types to add ledgerEntryId field
to invoices, transactions, receipts, and all *_mgt tables.
Do NOT run drizzle-kit push after this — DB already has the columns.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DONE CHECK — reply with:
"✅ PROMPT 1 DONE — tables created, columns added, indexes built"
and show the output of:
  npx tsx -e "
    import { Pool } from 'pg';
    const p = new Pool({ connectionString: process.env.DATABASE_URL });
    p.query(\`SELECT column_name FROM information_schema.columns
              WHERE table_name='account_ledger_entries'\`)
     .then(r => { r.rows.forEach(x => console.log(x.column_name)); p.end(); });
  "
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━