Before making any changes, carefully read and analyze all existing code first.
Do NOT modify anything until you have completed the full analysis below.

━━━ ANALYSIS PHASE (read only, no changes) ━━━

1. Find and read the Package Group API route file
   - Search for files containing "packageGroups" or "package-groups" in /server/src/routes/
   - Read the complete POST and PUT/PATCH handler code
   - List every field currently being read from req.body
   - List every field currently being written to the database

2. Find and read the Package Group form component
   - Search for files containing "PackageGroup" in /client/src/
   - Read the complete form field definitions
   - List every input field name and its current value
   - List what the form submits on save

3. Find and read the packages (sub-packages) API route
   - Search for the packages CRUD handlers
   - List every field currently being read from req.body
   - List every field currently being written to the database

4. Check the database schema
   - Read /db/schema.ts
   - List the exact column names for package_groups table
   - List the exact column names for packages table

5. Check the public landing page API
   - Find GET /api/public/packages handler
   - Read what fields it selects and returns
   - Check if it JOINs the packages sub-table

6. Report what you found BEFORE making any changes:
   - Which field names in the form do NOT match the DB columns
   - Which field names in the API handlers do NOT match the DB columns
   - Which fields exist in DB but are missing from the form
   - Which fields exist in the form but are missing from the DB schema
   - Any other potential issues you notice

━━━ WAIT — show me the analysis report first ━━━

After showing the analysis, list EXACTLY what you plan to change,
in this format:

  FILE: [filename]
  CHANGE: [what will be changed and why]
  RISK: [what could break if this goes wrong]
  SAFE: [how you will avoid breaking existing functionality]

Do not proceed to make changes until the analysis and change plan 
are shown.

━━━ CHANGE PHASE (only after analysis is complete) ━━━

Once analysis confirms the issues, apply ALL of the following fixes.
Make changes one file at a time. After each file, verify no syntax 
errors before moving to the next file.

── Fix 1: Backend API route for Package Groups ──────────────

In the Package Group create and update handlers:

DO NOT remove or rename any existing working fields.
ONLY fix fields that the analysis confirmed are mismatched.

The correct field mapping must be:
  req.body.nameEn       → DB column name_en
  req.body.nameKo       → DB column name_ko  
  req.body.nameJa       → DB column name_ja
  req.body.nameTh       → DB column name_th
  req.body.descriptionEn → DB column description_en
  req.body.descriptionKo → DB column description_ko
  req.body.descriptionJa → DB column description_ja
  req.body.descriptionTh → DB column description_th
  req.body.countryCode  → DB column country_code
  req.body.location     → DB column location
  req.body.thumbnailUrl → DB column thumbnail_url
  req.body.status       → DB column status
  req.body.campProviderId → DB column camp_provider_id

If any of these currently work correctly already, leave them as-is.
Only fix the ones the analysis identified as broken.

── Fix 2: Admin form field names ────────────────────────────

In the Package Group form component:

DO NOT change any field that is already working correctly.
ONLY fix fields that analysis confirmed are mismatched.

Change mismatched input field names to match DB column names:
  name="name"        → name="nameEn"        (if currently wrong)
  name="description" → name="descriptionEn" (if currently wrong)
  name="country"     → name="countryCode"   (if currently wrong)
  name="city"        → name="location"      (if currently wrong)

Add multilingual name/description fields if missing.
Group them under language tabs using the existing tab component:
  [🇦🇺 English] [🇰🇷 한국어] [🇯🇵 日本語] [🇹🇭 ภาษาไทย]
  
  Each tab contains:
    - Program Name field (nameEn / nameKo / nameJa / nameTh)
    - Description field  (descriptionEn / descriptionKo / descriptionJa / descriptionTh)
  
  English tab: required fields
  Other tabs: optional, show "(optional)" label

Add thumbnailUrl field if missing:
  Label: "Thumbnail Image URL"
  name="thumbnailUrl"
  placeholder="https://..."
  Helper text: "Paste an image URL (jpg, png, webp)"
  If URL has a value, show a small 120px wide preview image below

── Fix 3: Form default values on edit ───────────────────────

When loading an existing Package Group into the form for editing,
confirm the form populates these fields correctly:

  nameEn, nameKo, nameJa, nameTh,
  descriptionEn, descriptionKo, descriptionJa, descriptionTh,
  countryCode, location, thumbnailUrl, status

If the form currently uses wrong field names as keys 
(e.g. "name" instead of "nameEn"), fix the defaultValues object
to use the correct keys that match the input field names.

── Fix 4: Packages sub-table API ────────────────────────────

Apply the same analysis-then-fix approach to the packages 
(sub-packages) create/update API.

Correct field mapping:
  req.body.packageGroupId → DB column package_group_id
  req.body.name           → DB column name
  req.body.durationDays   → DB column duration_days
  req.body.maxParticipants → DB column max_participants
  req.body.priceAud       → DB column price_aud
  req.body.priceUsd       → DB column price_usd
  req.body.priceKrw       → DB column price_krw
  req.body.priceJpy       → DB column price_jpy
  req.body.priceThb       → DB column price_thb
  req.body.pricePhp       → DB column price_php
  req.body.priceSgd       → DB column price_sgd
  req.body.priceGbp       → DB column price_gbp
  req.body.status         → DB column status

Only fix mismatches. Leave working fields as-is.

── Fix 5: Public API completeness check ─────────────────────

In GET /api/public/packages:

Check if it already returns packages (sub-table) data.
If the packages sub-table JOIN is missing, add it so each 
package group includes its list of packages with prices.

Check if it filters by status = 'active'. 
If missing, add: WHERE package_groups.status = 'active'

Do not change anything else in this route.

━━━ VERIFICATION AFTER ALL CHANGES ━━━

After all fixes are applied, run these checks:

1. TypeScript compilation check:
   npx tsc --noEmit
   → Fix any type errors before proceeding

2. Confirm the server starts without errors:
   Check that no import errors or runtime crashes occur on startup

3. Database field verification:
   Run this query to confirm a newly created package group 
   saves correctly:
   
   SELECT id, name_en, name_ko, country_code, location, 
          thumbnail_url, status
   FROM package_groups 
   ORDER BY created_at DESC 
   LIMIT 3;

4. API response verification:
   GET /api/public/packages
   Confirm the response includes:
   - nameEn (not null for active records)
   - countryCode
   - location
   - packages array with at least one item containing prices

Report the results of each verification step.
If any verification fails, fix the issue before finishing.

━━━ DO NOT ━━━

- Do not delete any existing database columns
- Do not change any routes that are unrelated to package groups
- Do not modify authentication or permission logic
- Do not change any other admin pages (applications, contracts, etc.)
- Do not update the landing page UI yet — that is a separate step
