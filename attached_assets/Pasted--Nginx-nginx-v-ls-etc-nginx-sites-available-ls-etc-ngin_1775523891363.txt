$ # Nginx 버전 확인
nginx -v

# 현재 설정 파일 목록
ls /etc/nginx/sites-available/

# 현재 활성화된 설정 목록
ls /etc/nginx/sites-enabled/$ nginx version: nginx/1.18.0 (Ubuntu)
$ $ $ default  dev  production
$ $ $
camp.edubee.co  myhrlink.com.au  thaistudyhub.com  timest.jp  visagreen.au
dev.ebizlab.co  psynergy.com.au  timest.co         timest.kr
dev.smarty.one  smarty.one       timest.com.au     timest.tw
$ cat /etc/nginx/sites-enabled/camp.edubee.co
server {
    listen 80;
    server_name camp.edubee.co;
    root /srv/camp.edubee.co/public;

    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";

    index index.php;

    charset utf-8;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location = /favicon.ico { access_log off; log_not_found off; }
    location = /robots.txt  { access_log off; log_not_found off; }

    error_page 404 /index.php;

    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php8.4-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        fastcgi_param HTTPS HTTPS;
        include fastcgi_params;

        fastcgi_buffer_size 32k;
        fastcgi_buffers 16 32k;
        fastcgi_busy_buffers_size 64k;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}
$ cat /etc/nginx/sites-enabled/camp.edubee.co
server {
    listen 80;
    server_name camp.edubee.co;
    root /srv/camp.edubee.co/public;

    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";

    index index.php;

    charset utf-8;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location = /favicon.ico { access_log off; log_not_found off; }
    location = /robots.txt  { access_log off; log_not_found off; }

    error_page 404 /index.php;

    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php8.4-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        fastcgi_param HTTPS HTTPS;
        include fastcgi_params;

        fastcgi_buffer_size 32k;
        fastcgi_buffers 16 32k;
        fastcgi_busy_buffers_size 64k;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}
$ sudo nano /etc/nginx/sites-enabled/camp.edubee.co
$ sudo tee /etc/nginx/sites-enabled/camp.edubee.co > /dev/null << 'EOF'
server {
    listen 80;
    server_name camp.edubee.co;

    location / {
        proxy_pass https://edubee-crm-20260401.replit.app/;
        proxy_set_header Host edubee-crm-20260401.replit.app;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_ssl_server_name on;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
EOF> > > > > > > > > > > > > > > >
$ sudo tee /etc/nginx/sites-available/crm.edubee.co > /dev/null << 'EOF'
server {
    listen 80;
    server_name crm.edubee.co;

    location /admin/ {
        proxy_pass https://edubee-crm-20260401.replit.app/admin/;
        proxy_set_header Host edubee-crm-20260401.replit.app;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_ssl_server_name on;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    location / {
        return 301 /admin/;
    }
}
EOF> > > > > > > > > > > > > > > > > > > >
$ sudo tee /etc/nginx/sites-available/www.edubee.co > /dev/null << 'EOF'
server {
    listen 80;
    server_name www.edubee.co;

    location /website/ {
        proxy_pass https://edubee-crm-20260401.replit.app/website/;
        proxy_set_header Host edubee-crm-20260401.replit.app;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_ssl_server_name on;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    location / {
        return 301 /website/;
    }
}
EOF> > > > > > > > > > > > > > > > > > > >
$ sudo ln -s /etc/nginx/sites-available/crm.edubee.co /etc/nginx/sites-enabled/
sudo ln -s /etc/nginx/sites-available/www.edubee.co /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginxsudo ln -s /etc/nginx/sites-available/www.edubee.co /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx$
$