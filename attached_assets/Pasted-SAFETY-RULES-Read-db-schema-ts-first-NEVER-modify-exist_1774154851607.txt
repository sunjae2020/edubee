SAFETY RULES:
- Read /db/schema.ts first
- NEVER modify existing CAMP routes
- Report file list first, wait for "진행해"
- npx tsc --noEmit after every change

Create /server/src/routes/services-internship.ts:
  GET   /api/services/internship
  GET   /api/services/internship/:id
  PATCH /api/services/internship/:id

Create /server/src/routes/services-guardian.ts:
  GET   /api/services/guardian
  GET   /api/services/guardian/:id
  PATCH /api/services/guardian/:id
  POST  /api/services/guardian/:id/monthly-report
    append { month, reportText, sentAt, parentAcked } to monthlyReports JSONB array
  GET   /api/services/guardian/billing-due
    return guardians where billing is due this month based on billingCycle

Build /client/src/pages/services/InternshipPage.tsx:

Pipeline stages (horizontal):
  Profile Review → Company Matching → Interview → Confirmed → Working → Completed

Table columns: Student | Industry | Company | Position | Employment Type | Start Date | Status

Detail page — Tabs: Student Profile | Company Match | Progress | Documents
  Student Profile: englishLevel, workExperience JSONB cards, preferredIndustry chips
  Progress: resumePrepared toggle | coverLetterPrepared toggle | interviewDate | interviewResult | referenceLetterIssued toggle

Build /client/src/pages/services/GuardianPage.tsx:

Billing alert banner (show when count > 0):
  bg #FEF0E3
  "이번 달 가디언 청구 예정: {N}건, 총 A$XX,XXX [Process →]"

Table columns: Student | School | Guardian Staff | Billing Cycle | Service Period | Status

Detail page — Tabs: Details | Monthly Reports | School Events | Medical | Billing
  Monthly Reports: monthlyReports JSONB timeline
    [+ Add Report] → month, reportText, sentAt, parentAcked toggle
  Billing tab:
    list recurring invoices (isRecurring=true)
    [Generate This Month Invoice] button
      POST /api/accounting/invoices/recurring
        INSERT invoices { isRecurring: true, recurringSeq: prev+1, parentInvoiceId }

Verify:
npx tsc --noEmit
Add monthly report → SELECT monthly_reports FROM guardian_mgt ORDER BY updated_at DESC LIMIT 1;
Generate recurring invoice → SELECT recurring_seq FROM invoices ORDER BY created_on DESC LIMIT 1;
