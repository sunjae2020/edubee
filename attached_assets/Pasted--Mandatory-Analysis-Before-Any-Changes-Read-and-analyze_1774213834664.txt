## Mandatory Analysis Before Any Changes
Read and analyze all relevant files before making any modifications.
After completing the analysis, report findings first — do NOT make any changes until I explicitly say "proceed".

---

## Task Objective

Add a new **Camp Contract** menu item to the sidebar based on the existing **Contract** menu.

### Requirements
1. **Do NOT touch the existing Contract in any way** — its routes, components, DB schema, and API routes must remain exactly as they are
2. **Copy** the existing Contract's page, component, and route files to create brand-new dedicated Camp Contract files
3. The new Camp Contract menu item and route must be placed **directly below the Enrollment Sport item** in the sidebar
4. The Camp Contract page heading/title must display as "Camp Contract"

---

## Analysis Phase (Must complete before any modifications)

Read the following files in order and report findings:

1. **Read the sidebar navigation file**
   - Identify where the current Contract menu item is declared
   - Confirm the exact position, key, and label of the Enrollment Sport item
   - Understand the full menu order structure

2. **Read all existing Contract-related files**
   - Page component file (e.g. /client/src/pages/contracts/ or similar path)
   - Contract route declaration in the frontend router
   - Backend route file (e.g. /server/src/routes/contracts.ts or similar path)
   - contracts-related table in the DB schema (/db/schema.ts)

3. **Report analysis results in the following format:**
   - Sidebar file path + exact location of Contract and Enrollment Sport items
   - Contract page component file path
   - Contract frontend route path (URL path)
   - Contract backend API route file path
   - List of files to be created (source file → copy destination path)
   - Proposed URL path for Camp Contract (e.g. /camp-contracts)

---

## Modification Plan (Proceed only after I approve)

### Step 1 — Copy frontend page component
- Copy the existing Contract page file to create a new dedicated CampContract file
- Update the component name and page title to "Camp Contract" inside the copied file
- Do NOT modify the original Contract file in any way

### Step 2 — Add Camp Contract route to the frontend router
- Add a new route entry below the existing Contract route (do NOT modify the existing route)
- URL: /camp-contracts (or follow the naming convention confirmed during analysis)

### Step 3 — Add Camp Contract menu item to the sidebar
- Insert the Camp Contract menu item directly below the Enrollment Sport item
- Do NOT change the position or content of the existing Contract menu item
- Use the same icon as the existing Contract item (Lucide React, stroke-width 1.5)

### Step 4 — Copy backend API route (only if required)
- Only add a new backend route if the analysis determines it is necessary
- Do NOT modify the existing Contract API route under any circumstance

---

## Safety Rules During Modifications

**Always do:**
- Modify one file at a time and check for TypeScript errors after each change
- Keep all existing Contract-related files (page, route, API, schema) completely untouched
- Only modify files directly related to this task

**Never do:**
- Delete or rename any DB columns
- Modify authentication or authorization code
- Guess field names without reading the schema first
- Make bulk changes across multiple files at once

---

## Required Verification After All Changes

1. `npx tsc --noEmit` → Confirm 0 TypeScript errors
2. Confirm server starts cleanly → No import errors or runtime errors
3. Confirm Camp Contract menu appears directly below Enrollment Sport in the sidebar
4. Confirm /camp-contracts page loads correctly
5. Confirm the existing Contract menu and page still work exactly as before

---

## Completion Report Format

✅ Modified files: [list]
✅ Changes made: [description]
✅ Verification results: [tsc / server startup / sidebar position / page load / existing Contract still working — each result]
⚠️ Next steps: [if any]