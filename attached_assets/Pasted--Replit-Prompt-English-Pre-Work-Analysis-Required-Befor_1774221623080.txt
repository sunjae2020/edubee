## ✍️ Replit Prompt (English)

```
## Pre-Work Analysis (Required)

Before modifying any code, you must:
1. Read all related files in full
2. Confirm exactly what needs to be changed
3. Report the list of files to be modified and the planned changes
4. Do NOT make any changes until I explicitly say "proceed"

---

## Tasks

The current "Application" menu was built specifically for Camp.
Complete the following two tasks in order.

---

### Task 1 — Create "Camp Application" as a Copy

**Goal:** Duplicate the existing Application feature under the name "Camp Application".
The existing Application menu, pages, routes, and components must NOT be modified in any way.

**Files to analyse first:**
- `/client/src/pages/` — locate all Application-related page files
- `/client/src/components/` — locate all Application-related components
- `/server/src/routes/` — locate all Application-related API routes
- `/db/schema.ts` — confirm Application-related tables and fields
- Sidebar navigation component file

**Duplication rules:**
- Copy the existing Application page file → create a new CampApplication page file
- Copy the existing Application components → create new CampApplication components
- New route path: `/camp-application` (add new; keep existing `/application` route untouched)
- Update all page titles and UI labels: "Application" → "Camp Application"
- Do NOT modify a single character in any existing Application file
- Reuse the existing Application API endpoints as-is
  (Do NOT create new DB tables or columns)

---

### Task 2 — Reorder Sidebar Menu

**Goal:** Move "Camp Application" to appear directly above "Camp Contract" in the sidebar.

**Target menu order (within the Camp section):**
```
Camp Application   ← newly added in Task 1, placed directly above Camp Contract
Camp Contract
(all other menu items remain in their current order)
```

**Rules:**
- Only change the array order in the sidebar component
- Do NOT modify the label, path, or order of any other menu item

---

## Post-Change Verification (Required)

After all changes are complete, verify in this order:

1. `npx tsc --noEmit` → confirm 0 TypeScript errors
2. Server starts cleanly → no import errors or runtime errors
3. `/application` route → confirm existing Application page loads correctly
4. `/camp-application` route → confirm Camp Application page loads correctly
5. Sidebar → confirm "Camp Application" appears directly above "Camp Contract"

Report the result of each step.
If any step fails, do NOT mark the task as complete — fix it immediately.

---

## Completion Report Format

✅ Modified files: [list]
✅ Changes made: [description]
✅ Verification results: [tsc / server / route access / sidebar order — each result]
⚠️  Next steps: [if any]
```