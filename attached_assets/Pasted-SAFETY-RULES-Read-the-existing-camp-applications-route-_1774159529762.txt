SAFETY RULES:
- Read the existing camp_applications route file COMPLETELY before touching anything
- NEVER rename or delete existing columns or tables
- NEVER change existing route logic — only append new code after existing logic
- Report file list first, wait for "진행해"
- npx tsc --noEmit after every change

Step 1: Read these files and report:
  - Find camp_applications route in /server/src/routes/
  - Read it completely
  - Show current camp_applications table columns

Step 2: Add columns to camp_applications (if not already present)
  leadId:     uuid("lead_id").references(() => leads.id),
  contractId: uuid("contract_id").references(() => contracts.id),

  npx drizzle-kit generate && npx drizzle-kit push

Step 3: In the existing camp_applications PATCH route (status update),
  AFTER the existing status update logic completes, append:

  if (newStatus === 'reviewing' && !application.leadId) {
    const leadRef = 'LD-' + Date.now().toString(36).toUpperCase().padStart(8, '0');

    let contactId = null;
    const existing = await db.select().from(contacts)
      .where(eq(contacts.emailAddress, application.applicantEmail)).limit(1);

    if (existing.length > 0) {
      contactId = existing[0].id;
    } else {
      const [newContact] = await db.insert(contacts).values({
        firstName:    application.applicantName.split(' ')[0],
        lastName:     application.applicantName.split(' ').slice(1).join(' ') || '-',
        emailAddress: application.applicantEmail,
        mobileNumber: application.applicantPhone,
        nationality:  application.applicantNationality,
        ownerId:      currentUserId,
      }).returning();
      contactId = newContact.id;
    }

    const [newLead] = await db.insert(leads).values({
      leadRefNumber:   leadRef,
      leadStatus:      'In Progress',
      leadSource:      'Camp Application',
      inquiryType:     'Camp',
      contactId:       contactId,
      ownerId:         currentUserId,
      assignedStaffId: application.assignedStaffId,
      description:     `Camp Application ${application.applicationRef}`,
      status:          'Active',
    }).returning();

    await db.update(campApplications)
      .set({ leadId: newLead.id })
      .where(eq(campApplications.id, application.id));
  }

  if (newStatus === 'confirmed' && !application.contractId) {
    const name = `CT-${application.applicantName}-${new Date().toISOString().split('T')[0]}`;
    const [newContract] = await db.insert(contracts).values({
      name:                    name,
      contractStatus:          'Pending',
      serviceModulesActivated: ['camp', 'pickup', 'accommodation'],
      ownerId:                 currentUserId,
      status:                  'Active',
    }).returning();

    await db.update(campApplications)
      .set({ contractId: newContract.id })
      .where(eq(campApplications.id, application.id));
  }

Step 4: In /admin/crm/leads/:id detail page,
  when lead.leadSource === 'Camp Application',
  fetch camp_applications where leadId = lead.id
  display card: Application Ref | Package | Preferred Start | Status | [View Application →]

Verify:
npx tsc --noEmit
Change a camp application status to 'reviewing' →
  SELECT * FROM leads WHERE lead_source = 'Camp Application' ORDER BY created_on DESC LIMIT 1;
  SELECT ca.application_ref, ca.lead_id FROM camp_applications WHERE lead_id IS NOT NULL ORDER BY updated_at DESC LIMIT 1;
Change status to 'confirmed' →
  SELECT * FROM contracts ORDER BY created_on DESC LIMIT 1;
Confirm all existing CAMP application functionality still works
