Here is the complete single English Replit prompt:

---

```
## ⚠️ MANDATORY PRE-WORK — READ BEFORE TOUCHING ANY CODE

Before writing a single line of code, you MUST complete the following analysis steps:

1. Read ALL related files in full — do not skim
2. Identify exactly what exists and what needs to be created
3. Report back to me with: (a) files you will modify, (b) files you will create, (c) exact changes per file
4. DO NOT proceed until I explicitly say "go ahead"
   → Exception: if I say "just do it", proceed immediately

---

## PROJECT CONTEXT

**Edubee CRM** is a SaaS platform for education agencies (study abroad, camp, internship management).

- Stack: React + TypeScript + Vite (frontend) / Node.js + Express + Drizzle ORM + PostgreSQL (backend)
- Platform: Replit (Beta/MVP stage)
- Core principle: Never break existing working code

Key file locations:
- DB Schema: `/db/schema.ts`
- Backend routes: `/server/src/routes/`
- Frontend pages: `/client/src/pages/`
- Components: `/client/src/components/`
- Environment: `.env` (DATABASE_URL, SUPABASE_URL, etc.)

---

## TASK OVERVIEW

Add a new **"Products"** category to the sidebar navigation containing 4 sub-menu items:

| Sub-menu | Route | Status |
|---|---|---|
| Products Group | `/products/groups` | NEW |
| Products Type | `/products/types` | NEW |
| Products | `/products` | MOVE existing page here |
| Promotion | `/products/promotions` | NEW |

---

## STEP 1 — ANALYSIS (do this first, report before coding)

Read the following files and report findings:

1. Sidebar/navigation component files in `/client/src/components/` — find where nav items are defined
2. `/client/src/App.tsx` or router file — find existing Products route path
3. All existing product-related files in `/client/src/pages/`
4. `/db/schema.ts` — confirm field names for: `product_groups`, `product_types`, `products`, `commissions`, `promotions`
5. `/server/src/routes/` — check if any product-related routes already exist

Report format:
- Current Products page path: [path]
- Current Products route: [route]
- Existing product API routes: [list or "none"]
- Files to be modified: [list]
- Files to be created: [list]

---

## STEP 2 — DATABASE SCHEMA REFERENCE

**USE THESE EXACT FIELD NAMES. Do not guess. Always read `/db/schema.ts` to confirm.**

### product_groups
```
id            UUID PK
name          VARCHAR(255) NOT NULL
description   TEXT
status        VARCHAR(20)  DEFAULT 'Active'   -- 'Active' | 'Inactive'
created_on    TIMESTAMP    DEFAULT NOW()
modified_on   TIMESTAMP    DEFAULT NOW()
```

### product_types
```
id                  UUID PK
name                VARCHAR(255) NOT NULL
product_group_id    UUID FK → product_groups
service_module_type VARCHAR(50)   -- study_abroad | pickup | accommodation | internship | settlement | guardian | camp
description         TEXT
status              VARCHAR(20)  DEFAULT 'Active'
created_on          TIMESTAMP    DEFAULT NOW()
modified_on         TIMESTAMP    DEFAULT NOW()
```

### products (key fields)
```
id                    UUID PK
name                  VARCHAR(255) NOT NULL
manual_input          BOOLEAN      DEFAULT FALSE
from_date             DATE
to_date               DATE
duration_weeks        INTEGER
category_1_id         UUID FK → product_groups       -- Product Group
category_2_id         UUID FK → accounts              -- Provider Account
item_description      TEXT
price                 DECIMAL(12,2) NOT NULL DEFAULT 0
is_gst_included       BOOLEAN      DEFAULT FALSE
currency              VARCHAR(20)  DEFAULT 'AUD'
installment_plan      VARCHAR(50)  -- None | Weekly | Monthly | Termly
default_payment_term  VARCHAR(50)  -- Once | Multiple Times | Monthly
number_of_payments    INTEGER
minimum_payment       DECIMAL(12,2)
product_priority      INTEGER
service_module_type   VARCHAR(50)  -- study_abroad | pickup | accommodation | internship | settlement | guardian | camp | institute | tour
is_recommend          BOOLEAN      DEFAULT FALSE
provider_id           UUID FK → accounts
commission_id         UUID FK → commissions
promotion_id          UUID FK → promotions
display_on_quote      BOOLEAN      DEFAULT TRUE
display_on_invoice    BOOLEAN      DEFAULT TRUE
tax_rate_id           UUID FK → tax_rates
product_type_id       UUID FK → product_types
status                VARCHAR(20)  DEFAULT 'Active'
created_on            TIMESTAMP    DEFAULT NOW()
modified_on           TIMESTAMP    DEFAULT NOW()
```

### commissions
```
id              UUID PK
name            VARCHAR(255) NOT NULL
commission_type VARCHAR(20)  -- percentage | fixed
rate            DECIMAL(8,4) -- e.g. 0.1500 = 15%
amount          DECIMAL(12,2)
base            VARCHAR(30)  -- gross | net
payment_method  VARCHAR(20)  -- pre_deduct | invoice
school_id       UUID FK → accounts
status          VARCHAR(20)  DEFAULT 'Active'
created_on      TIMESTAMP    DEFAULT NOW()
modified_on     TIMESTAMP    DEFAULT NOW()
```

### promotions
```
id               UUID PK
name             VARCHAR(255) NOT NULL
product_id       UUID FK → products
account_id       UUID FK → accounts    -- Provider
from_date        DATE
to_date          DATE
promotion_price  TEXT    -- free-text, e.g. "$320/week", "10% OFF"
status           VARCHAR(20)  DEFAULT 'Active'
created_on       TIMESTAMP    DEFAULT NOW()
modified_on      TIMESTAMP    DEFAULT NOW()
```

---

## STEP 3 — SIDEBAR NAVIGATION UPDATE

Add a new "PRODUCTS" section to the sidebar.
Do NOT change any existing sections, styles, or item order.

Reference the existing sidebar's section label style and nav item pattern — match it exactly.

Section structure to add:
```
── PRODUCTS ──────────────────────
  [icon]  Products Group     →  /products/groups
  [icon]  Products Type      →  /products/types
  [icon]  Products           →  /products
  [icon]  Promotion          →  /products/promotions
```

Icons (use Lucide React, strokeWidth 1.5, size 20):
- Products Group: `Layers`
- Products Type: `Tag`
- Products: `Package`
- Promotion: `Percent`

Sidebar design rules (match exactly with existing):
- Section label: 11px / weight 500 / UPPERCASE / letter-spacing 0.08em / color #A8A29E / margin 16px 16px 4px
- Nav item: height 40px / padding 0 16px / border-radius 8px / font 14px / color #57534E
- Active state: background #FEF0E3 / color #F5821F / font-weight 600
- Hover state: background #F4F3F1 / color #1C1917

---

## STEP 4 — ROUTE CHANGES

### Move existing Products page
- Find the current Products route (e.g., `/products` or `/admin/products` — confirm from App.tsx)
- Move it to `/products` if not already there
- If path changes, update all internal links referencing the old path
- Do NOT change any logic inside the Products page component

### Add 3 new routes
```
/products/groups      →  ProductGroupsPage  (new component)
/products/types       →  ProductTypesPage   (new component)
/products/promotions  →  PromotionsPage     (new component)
```

---

## STEP 5 — BACKEND API ENDPOINTS

Check `/server/src/routes/` first. If any of these already exist, do not recreate them.

Follow the existing route file pattern exactly (same error handling, response format, middleware).

### Product Groups  (`/api/product-groups`)
```
GET    /api/product-groups         List all (supports ?status= filter)
POST   /api/product-groups         Create new
GET    /api/product-groups/:id     Get single record
PUT    /api/product-groups/:id     Update
DELETE /api/product-groups/:id     Set status = 'Inactive' (NO hard delete)
```

### Product Types  (`/api/product-types`)
```
GET    /api/product-types          List all (supports ?product_group_id= and ?service_module_type= filters)
POST   /api/product-types          Create new
GET    /api/product-types/:id      Get single record
PUT    /api/product-types/:id      Update
DELETE /api/product-types/:id      Set status = 'Inactive'
```
→ All GET responses must JOIN and include `product_group` name

### Promotions  (`/api/promotions`)
```
GET    /api/promotions             List all (supports ?product_id=, ?status=, ?from_date=, ?to_date= filters)
POST   /api/promotions             Create new
GET    /api/promotions/:id         Get single record
PUT    /api/promotions/:id         Update
DELETE /api/promotions/:id         Set status = 'Inactive'
```
→ All GET responses must JOIN and include `product` name and `account` name (provider)

---

## STEP 6 — FRONTEND PAGES (NEW)

Build each new page following the existing page component patterns in `/client/src/pages/`.

---

### Page A: Products Group  (`/products/groups`)

**List View (default)**

Page header:
- Title: "Products Group"
- Subtitle: "Manage product categories for your service offerings"
- Top-right: "+ Add Product Group" button (Primary style)

Table columns:
| # | Column | Source |
|---|--------|--------|
| 1 | Name | `product_groups.name` |
| 2 | Description | `product_groups.description` (truncate at 60 chars) |
| 3 | Products | Count of linked products (JOIN) |
| 4 | Status | Badge — Active (green) / Inactive (grey) |
| 5 | Created | `created_on` formatted as DD MMM YYYY |
| 6 | Actions | Edit icon / Deactivate toggle |

Toolbar (above table):
- Search input: placeholder "Search by name..."
- Status filter dropdown: All / Active / Inactive (default: Active)

**Add / Edit Modal**

Title: "Add Product Group" / "Edit Product Group"

Fields:
- Name * (required, text input)
- Description (textarea, 3 rows)
- Status: radio or toggle — Active / Inactive (default: Active)

Footer buttons:
- "Save Product Group" (Primary)
- "Cancel" (Secondary)

Validation: Name is required. Show inline error if empty on submit.

---

### Page B: Products Type  (`/products/types`)

**List View (default)**

Page header:
- Title: "Products Type"
- Subtitle: "Define product types and map them to service automation modules"
- Top-right: "+ Add Product Type" button (Primary)

Table columns:
| # | Column | Source |
|---|--------|--------|
| 1 | Name | `product_types.name` |
| 2 | Product Group | Joined from `product_groups.name` via `product_group_id` |
| 3 | Service Module | `service_module_type` shown as readable label (see mapping below) |
| 4 | Products | Count of linked products (JOIN) |
| 5 | Status | Badge — Active / Inactive |
| 6 | Actions | Edit / Deactivate |

Service module display labels:
```
study_abroad   → "Study Abroad"
pickup         → "Pickup"
accommodation  → "Accommodation"
internship     → "Internship"
settlement     → "Settlement"
guardian       → "Guardian"
camp           → "Camp"
(null/none)    → "—"
```

Toolbar: Search by name + Product Group dropdown filter + Status filter

**Add / Edit Modal**

Title: "Add Product Type" / "Edit Product Type"

Fields:
- Name * (required)
- Product Group * → Dropdown (load from GET /api/product-groups?status=Active)
- Service Module Type → Dropdown (single select, nullable)
  Options:
  ```
  — None —
  Study Abroad     (value: study_abroad)
  Pickup           (value: pickup)
  Accommodation    (value: accommodation)
  Internship       (value: internship)
  Settlement       (value: settlement)
  Guardian         (value: guardian)
  Camp             (value: camp)
  ```
  Helper text below field: "When a contract is created with this product type, the corresponding service management record will be automatically generated."
- Description (textarea)
- Status: Active / Inactive

---

### Page C: Products  (`/products`)

This is the EXISTING Products page being moved to a new route.

**DO NOT rewrite this page.** Only:
1. Update the route path if it changed
2. If the existing page is missing any of the fields listed below, add them — otherwise leave everything as-is

Verify these connections exist in the Products form. If missing, add them:

| Field | Connects to |
|---|---|
| Product Group | `category_1_id` → `product_groups` (dropdown, Active only) |
| Provider Account | `provider_id` / `category_2_id` → `accounts` (School/Partner type preferred) |
| Product Type | `product_type_id` → `product_types` (dropdown, Active only) |
| Commission | `commission_id` → `commissions` (dropdown, Active only) |
| Promotion | `promotion_id` → `promotions` (dropdown, Active + valid date range) |

Products list columns to verify exist:
Name | Product Type | Provider | Price | Currency | Status | Actions

---

### Page D: Promotion  (`/products/promotions`)

**List View (default)**

Page header:
- Title: "Promotions"
- Subtitle: "Manage promotional pricing for products"
- Top-right: "+ Add Promotion" button (Primary)

Table columns:
| # | Column | Source |
|---|--------|--------|
| 1 | Name | `promotions.name` |
| 2 | Product | Joined product name |
| 3 | Provider | Joined account name |
| 4 | Period | `from_date` ~ `to_date` formatted as "DD MMM YYYY – DD MMM YYYY" |
| 5 | Promo Price | `promotion_price` |
| 6 | Status | Smart badge (see logic below) |
| 7 | Actions | Edit / Deactivate |

Status badge logic:
- `status = 'Inactive'` → Grey "Inactive" badge
- `status = 'Active'` AND today is within `from_date`~`to_date` → Orange "In Progress" badge (`bg #FEF0E3 / text #F5821F`)
- `status = 'Active'` AND `to_date` < today → Grey "Expired" badge
- `status = 'Active'` AND `from_date` > today → Yellow "Upcoming" badge

Toolbar: Search by name + Product dropdown filter + Status filter + Date range filter (from/to)

**Add / Edit Modal**

Title: "Add Promotion" / "Edit Promotion"

Fields:
- Name * (required)
- Product * → Searchable dropdown (GET /api/products?status=Active)
  → On selection: auto-populate Provider Account field from selected product's `provider_id`
- Provider Account → Read-only text or locked dropdown (auto-filled from Product selection above)
- From Date / To Date → Date pickers (range, From must be before To)
- Promotion Price * (text input, free-format)
  Placeholder: "e.g. $320/week, 10% OFF first term, Free enrollment fee"
- Status: Active / Inactive

---

## STEP 7 — DESIGN SYSTEM (apply to ALL new components)

Match 100% with the existing Edubee CRM design. These are non-negotiable.

**Colors**
```
Primary Accent:    #F5821F  (buttons, active nav, links)
Accent Dark:       #D96A0A  (hover on primary buttons)
Accent Light:      #FEF0E3  (active nav bg, tag backgrounds)
Neutral 50:        #FAFAF9  (page background)
Neutral 100:       #F4F3F1  (sidebar, card backgrounds)
Neutral 200:       #E8E6E2  (borders, dividers)
Neutral 400:       #A8A29E  (placeholder, disabled)
Neutral 600:       #57534E  (labels, secondary text)
Neutral 900:       #1C1917  (primary text, headings)
Success:           #16A34A  (Active status)
Warning:           #CA8A04  (Pending)
Danger:            #DC2626  (Inactive, error)
```

**Buttons**
```
Primary:   bg #F5821F / text white / radius 8px / hover: bg #D96A0A, translateY(-1px), shadow rgba(245,130,31,0.25)
Secondary: bg white / border 1.5px #E8E6E2 / radius 8px / hover: border #A8A29E
Ghost:     transparent / color #57534E / hover: bg #F4F3F1
Danger:    bg #FEF2F2 / color #DC2626 / border #FECACA / hover: bg #DC2626 / color white
```

**Inputs**
```
height: 40px / border: 1.5px solid #E8E6E2 / radius 8px / padding 0 12px
focus: border #F5821F / box-shadow 0 0 0 3px rgba(245,130,31,0.15)
placeholder: #A8A29E
```

**Table**
```
Header row: bg #FAFAF9 / 12px / weight 500 / uppercase / color #57534E
Body row: 48px height / border-bottom 1px #F4F3F1 / hover bg #FAFAF9
Selected row: bg #FEF0E3
```

**Status Badges**
```
Active:      bg #DCFCE7 / text #16A34A   (12px / weight 500 / radius 999px / padding 3px 10px)
Inactive:    bg #F4F3F1 / text #57534E
In Progress: bg #FEF0E3 / text #F5821F
Upcoming:    bg #FEF9C3 / text #CA8A04
Expired:     bg #F4F3F1 / text #A8A29E
```

**Cards**
```
bg white / border 1px #E8E6E2 / radius 12px / padding 20px 24px
shadow: 0 1px 3px rgba(0,0,0,0.06)
```

**Modals**
```
border-radius: 16px / overlay: rgba(0,0,0,0.4)
open animation: scale(0.97→1) + opacity(0→1) 200ms ease
```

**Typography: "Inter" font, imported from Google Fonts**
```
Page title:   32px / weight 700 / line-height 1.2
Section head: 24px / weight 600
Card title:   18px / weight 600
Body:         14px / weight 400 / line-height 1.6
Small/meta:   12px / weight 400
Labels:       12px / weight 500 / UPPERCASE / letter-spacing 0.05em
```

**Icons: Lucide React — strokeWidth: 1.5, size: 16px or 20px**

---

## STEP 8 — SAFETY RULES (strictly enforced)

✅ MUST DO:
- Read all related files before making any changes
- Modify one file at a time, check for TypeScript errors after each change
- Follow existing code patterns for routes, components, API structure
- JOIN related tables in API responses (never return raw IDs without names)

🚫 NEVER DO:
- Delete or rename any DB column
- Modify any file unrelated to this task
- Modify authentication or permission logic
- Guess field names without reading the schema
- Bulk-modify multiple files in one go
- Hard-delete any record (always use `status = 'Inactive'`)

---

## STEP 9 — VERIFICATION CHECKLIST

After all changes are complete, run and report results for each item:

**1. TypeScript**
```
npx tsc --noEmit
```
→ Must return 0 errors

**2. Server startup**
```
Check server starts without import errors or runtime crashes
```

**3. Database queries**
```sql
SELECT * FROM product_groups ORDER BY created_on DESC LIMIT 3;
SELECT pt.*, pg.name as group_name FROM product_types pt LEFT JOIN product_groups pg ON pt.product_group_id = pg.id LIMIT 3;
SELECT pr.*, p.name as product_name, a.name as provider_name FROM promotions pr LEFT JOIN products p ON pr.product_id = p.id LEFT JOIN accounts a ON pr.account_id = a.id LIMIT 3;
```

**4. API endpoint responses**
```
GET /api/product-groups              → returns array, each item has: id, name, description, status
GET /api/product-types               → returns array, each item has: id, name, product_group_id, product_group_name, service_module_type
GET /api/promotions                  → returns array, each item has: id, name, product_id, product_name, account_id, account_name, from_date, to_date, promotion_price
```

**5. UI Checks**
- Sidebar shows "PRODUCTS" section with 4 sub-items
- All 4 routes render without crashing
- Add modal opens and closes correctly on all 3 new pages
- Products page still functions (existing features intact)

---

## COMPLETION REPORT FORMAT

When all work is done, report in this format:

```
✅ Files Modified: [list]
✅ Files Created: [list]
✅ Changes Made: [description per file]
✅ Verification Results:
   - TypeScript: [0 errors / X errors found]
   - Server startup: [OK / Error: ...]
   - DB queries: [OK / Error: ...]
   - API responses: [OK / Error: ...]
   - UI checks: [OK / Issues: ...]
⚠️  Next steps: [if any remaining work]
```
```