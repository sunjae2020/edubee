Standardize row hover highlighting across ALL list/table pages in the Edubee Camp platform.

REQUIREMENT:
Every list page, table, and data row throughout the entire app must use a consistent 
hover highlight style — a light orange background — matching the design system already 
in use on existing list pages.

HOVER STYLE (apply uniformly):
  background-color: #FEF0E3   ← Accent Light (orange tint)
  transition: background-color 150ms ease
  cursor: pointer (if row is clickable)

APPLY TO ALL of the following pages and components:
  - /admin/applications        (Applications list)
  - /admin/contracts           (Contracts list)
  - /admin/leads               (Leads list / Kanban list view)
  - /admin/package-groups      (Package Groups list)
  - /admin/packages            (Packages list)
  - /admin/products            (Products list)
  - /admin/users               (Users list)
  - /admin/services/institute  (Institute MGT list)
  - /admin/services/hotel      (Hotel MGT list)
  - /admin/services/pickup     (Pickup MGT list)
  - /admin/services/tour       (Tour MGT list)
  - /admin/services/settlement (Settlement MGT list)
  - /admin/services/interviews (Interviews list)
  - /admin/accounting/client-invoices
  - /admin/accounting/agent-invoices
  - /admin/accounting/partner-invoices
  - /admin/accounting/receipts
  - /admin/accounting/transactions
  - /admin/accounting/exchange-rates
  - /admin/my-accounting/settlements
  - /admin/my-accounting/invoices
  - /admin/my-accounting/revenue
  - /admin/reports             (Reports list)
  - /admin/my-programs         (Program list for parent_client)
  - /admin/notifications       (Notifications list)
  - /admin/settings/impersonation-logs
  - Any other table or list component across the platform

IMPLEMENTATION:
1. If using Tailwind CSS, apply: hover:bg-[#FEF0E3] transition-colors duration-150
2. If using inline styles or CSS modules, set: 
     tr:hover, .list-row:hover, .table-row:hover { background-color: #FEF0E3; }
3. Create or update a shared CSS utility class (e.g. .row-hover) and apply it 
   consistently to every <tr> or list row element across all components.
4. Do NOT override rows that have a selected/active state — selected rows keep 
   background: #FEF0E3 with font-weight: 600 (already defined in design system).
5. Ensure the hover does NOT apply to header rows (thead tr).

DESIGN SYSTEM REFERENCE:
  - Accent Light: #FEF0E3  ← row hover background
  - Selected row: #FEF0E3 + font-weight 600
  - Table header row background: #FAFAF9 (no hover change)
  - Transition: all 200ms cubic-bezier(0.4, 0, 0.2, 1)

Verify the hover highlight works consistently across all pages listed above 
before completing.